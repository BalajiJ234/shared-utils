function sendPushNotification(deviceToken, message) {
  // Example push notification logic (pseudo code)
  console.log(`Sending push notification to ${deviceToken}: ${message}`);
}

module.exports = {
  sendPushNotification,
};
